[build]
  publish = "dist"
  command = "npm run build"

[build.environment]
  NODE_VERSION = "18"

# SPA fallback only - no other redirects
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
