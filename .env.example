# No public runtime secrets are required by the website.
#
# Analytics IDs are intentionally versioned in src/config/analytics.ts because
# they are public browser identifiers, not credentials. Add future server-side
# secrets only in Netlify/GitHub settings; never commit their values here.
