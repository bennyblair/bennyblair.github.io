# September release integration

The architectural release incorporates production main through `65ca29ab2cef5ab5426cda5cff7da06b696dc058`. It preserves the current two-step transaction enquiry, attribution contracts, updated property-finance guidance, 52 repaired guide headings, and nine restored production image paths.

The named architectural layouts, searchable guide directory, native mobile navigation, photography and cylinder sequence remain. Homepage route activation starts immediately. The compact mobile cylinder reserves its layout before first paint, while JavaScript-disabled visitors retain the full readable sequence.

The release comparison against freshly captured production HTML found all 380 sitemap routes, no removed routes or orphan pages, no changed canonical/title/description/H1/robots values, no broken local images, and no broken contents fragments. Three case-study schema image corrections remain documented exceptions. The homepage scenario heading and disclaimer use the approved design wording, retaining the distinction from completed client transactions.

The latest source passed 115 unit and contract tests, type checking, lint (nine existing warnings), content preservation for 282 articles, the SEO registry and protected-cohort gates, and the production dependency audit. Further browser and performance results are recorded in the release handoff after validation.

Seven inherited lender-count statements still require the owner's wording decision; the claim gate has not been disabled or relaxed. Production promotion requires all release checks to pass. Private preview isolation remains controlled by its build flag, separate from the normal production build.
